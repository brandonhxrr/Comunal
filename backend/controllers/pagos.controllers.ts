import { User, Comunidad } from "../entities";
import { Request, Response } from "express";
import { RequestUser } from "../interfaces";

export const makePago = async (req: RequestUser, res: Response) => {};

export const getPagosByComunidad = async (
  req: RequestUser,
  res: Response
) => {};
